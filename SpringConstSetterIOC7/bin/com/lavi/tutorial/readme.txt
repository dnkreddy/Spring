IOC : combination of setter and constructor based

But preferred either one among two.

@Resource - If bean config methodname and properties names are different
then use @Resource annotation and give name to bean as property name
@Bean(name="propName")
